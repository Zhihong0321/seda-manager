---
description: Auto-deploy the codebase to Railway via GitHub
---
// turbo-all

1. Stage all modified files using `git add .`
2. Commit the changes using `git commit -m "[Descriptive commit message]"`
3. Push the changes to GitHub using `git push` to trigger the Railway deployment.
